Brute force:
class Solution {
    public int characterReplacement(String s, int k) { 
        int maxlen=0;
        for(int i=0;i<s.length();i++){
            int[] freq=new int[26];
            int maxfreq=0;
            for(int j=i;j<s.length();j++){
                freq[s.charAt(j)-'A']++;
                maxfreq=Math.max(maxfreq,freq[s.charAt(j)-'A']);
                int changes=j-i+1;
                if(changes-maxfreq<=k) maxlen=Math.max(maxlen,changes);
                else break;
            }
        }
        return maxlen;
    }
}
Optimized version: Sliding window
class Solution {
    public int characterReplacement(String s, int k) {
        int[] freq=new int[26];
        int maxfreq=0;
        int maxlen=0;
        int left=0;
        for(int right=0;right<s.length();right++){
            char c=s.charAt(right);
            freq[c-'A']++;
            maxfreq=Math.max(maxfreq,freq[c-'A']);
            int changes=right-left+1;
            if(changes-maxfreq>k){
                freq[s.charAt(left)-'A']--;
                left++;
            }
            maxlen=Math.max(maxlen,right-left+1);
        }
        return maxlen;
    }
}
